# Server Configuration
HOST=0.0.0.0
PORT=8000

# Security & CORS (Comma-separated list of allowed origins, or * for all)
ALLOWED_ORIGINS=*,http://localhost:5173

# Application Settings
MAX_UPLOAD_MB=16

# Hugging Face Settings (Optional - uncomment and set custom path if needed)
# HF_HOME=/custom/path/to/huggingface/cache
